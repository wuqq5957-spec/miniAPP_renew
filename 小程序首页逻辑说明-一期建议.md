# 剑琅美遇小程序首页逻辑说明（一期建议）

## 一、范围与规则状态

本文用于确认小程序首页的城市、定位、搜索、活动推荐、门店筛选、排序、分页及卡片展示逻辑。

已确定的核心规则：

- 首页是平台门店列表，没有“当前门店”的概念；点击门店进入对应店铺详情。
- 城市选择支持“全国”，全国范围允许查看全部符合展示条件的门店。
- 只有开启 LBS 定位且成功获得有效定位结果时，才展示门店距离；未授权定位、定位失败、仅有收货地址等情况均不展示距离。
- 门店优先展示“我的门店”，再优先就近门店；就近排序的参考位置优先使用 LBS，收货地址次之。
- 分类为：全部、美发、美容、美甲美睫、我的门店；标签单选。

## 二、左上角城市如何确定

### 2.1 浏览范围与客户真实定位分开

- 左上角展示的是“浏览范围”：全国或某个城市，不保证等于用户实际所在城市。
- 城市弹层中的“当前定位”只表示实际 LBS 定位结果，不能使用历史城市或收货地址冒充。
- 选择“全国”后，左上角显示“全国”；即使真实位置在上海，也继续保持全国浏览范围。
- 只使用LBS定位标记用户真实位置， 不以城市中心点、默认门店或历史访问门店作为用户真实位置。

### 2.2 首次进入及重新打开

按以下顺序初始化：

1. 默认进入：全国
3. 否则，本次 LBS 定位成功：进入定位所在城市。
4. 未授权或定位失败：恢复上次浏览的城市。
5. 没有上次浏览城市：使用已登录用户默认收货地址所在城市。
6. 以上均不可用：默认全国，不阻断浏览，也不强制选择城市。

保留“全国”选择是为了避免用户希望查看全平台门店，却每次重新打开都被自动切回定位城市。其他城市的自动初始化顺序为一期建议。

### 2.3 手动选择、返回和重新定位

- 本次访问期间，手动选择的全国或城市优先，不被后台定位刷新自动覆盖。
- 从店铺详情返回首页，保留浏览范围、搜索词、分类、已加载列表和滚动位置。
- 手动切换浏览城市后，分类恢复“全部”，列表从第一页加载。
- 点击“重新定位”，成功后切换至实际定位城市，同时更新推荐结果；这是用户主动发起的范围变更。重新定位失败时保留原浏览范围，提示“定位失败，请重试或手动选择城市”。
- 未授权展示“未开启定位”；
- 已授权但未取得有效结果展示“暂未获取定位”，提供重试或手动选择入口。

## 三、城市与定位分别控制哪些区域

| 条件 | 控制范围 |
| --- | --- |
| 全国／城市 | 门店列表、门店搜索、首页活动推荐的地域范围 |
| LBS 坐标 | 实际门店距离、就近排序 |
| 默认收货地址坐标 | 无可用 LBS 时辅助门店就近排序，不用于距离展示 |
| 分类标签 | 仅筛选下方门店列表 |
| 搜索词 | 仅搜索门店列表；有搜索词时隐藏上方活动推荐 |

### 3.1 距离展示条件

同时满足以下条件才显示距离：

1. 用户已开启 LBS 定位权限。
2. 本次取得有效定位结果。
3. 对应门店有有效坐标，能够计算距离。

不满足任一条件，直接隐藏距离字段及分隔符，不显示“0m”“距离未知”或收货地址距离。此规则同时适用于门店列表和推荐活动卡片。

距离使用用户 LBS 到实际门店的直线距离，不是导航路程。全国或跨城市浏览时，只要上述条件满足，也可显示真实距离，不伪造“附近”。

### 3.2 排序位置与展示距离分开

- 一次查询统一使用一个参考位置：有效 LBS 优先，其次默认收货地址坐标，最后为无参考位置。
- 收货地址只使用用户已有的默认地址，不随机选择其他地址；没有可用默认地址时跳过。
- 不能某家店使用 LBS、另一家使用收货地址混合计算后比较距离。
- 个别门店缺少坐标时，作为距离不可计算处理，不切换到另一套位置来源。
- 用户关闭 LBS 后停止使用该定位结果并隐藏距离；若有默认收货地址，仍可辅助排序。

例：用户在上海浏览杭州，门店范围限定杭州。若 LBS 有效，列表可显示用户到杭州门店的真实距离；

## 四、搜索框

### 4.1 搜索范围

- 全国：搜索全国范围内符合展示条件的门店。
- 指定城市：只搜索该城市内的门店，不自动混入外地结果。
- 搜索与所选分类同时生效，例如“上海＋美发＋大悦城”。
- 搜索结果沿用门店列表排序，一期不叠加复杂的搜索相关度权重。

### 4.2 匹配字段

一期按以下字段进行关键词包含匹配：

- 门店名称。
- 详细地址。

支持名称中的部分文字，忽略首尾空格，英文不区分大小写。不匹配价格、评分、活动名称、评价内容；一期不做拼音、错别字纠正或语义搜索。

输入时可做约 300 毫秒防抖；每次有效搜索条件变化，从第一页查询，并避免旧请求结果覆盖新关键词的结果。清空搜索后恢复该浏览范围和分类下的门店列表。

### 4.3 无结果

- 文案：“未找到相关门店”。
- 提供“清空搜索”；指定城市时可增加“查看全国门店”。
- 用户点击“查看全国门店”才扩大范围，可保留关键词继续查找；不静默扩大搜索范围。

## 五、精选活动推荐

### 5.1 基础准入

活动需要同时满足：
- 所属实际服务门店正常上架。活动已上架，处于可购买时间内。
- 指定城市时，实际适用门店必须在该城市；全国时不限制城市。

### 5.2 推荐候选范围

根据用户当前所选城市/全国， 展示城市/全国范围内的活动；

### 5.3 推荐排序及数量

在确定的候选范围内：

1. 近 30 天有效销量从高到低。
2. 销量相同时，按上架时间由新至旧，最后用活动编号保证稳定顺序。

有效销量建议按已支付成功的购买份数统计。

- 同一家门店最多 2 个活动。
- 整个模块最多 10 个，横向滑动，不做无限分页。
- 活动不足10个时按实际数量展示，不复制凑数。

### 5.4 缺省与异常

- 城市活动不足，<=2个：自动扩展推荐范围至全国
- 全国推荐活动不足，即平台目前整体的活动数量<=2个：整块隐藏，收起多余分隔和占位。
- 活动已失效/下架：刷新后移除；

### 5.5 跳转

- 点击活动卡片， 跳转至活动详情页

## 六、门店列表筛选规则

### 6.1 基础范围

- 全国：全部城市正常的门店。
- 城市：上述门店中，地址属于所选城市的门店。
- 日常休息中的门店仍展示；休息不等于下架。
- 永久关闭、停用或被下架的门店不进入普通推荐列表。历史资产和订单仍应通过“我的”保留查询入口，不因列表隐藏而消失。

### 6.2 标签单选

| 标签 | 筛选规则 |
| --- | --- |
| 全部 | 不限制行业和办卡关系 |
| 美发 | 门店行业包含美发 |
| 美容 | 门店行业包含美容 |
| 美甲美睫 | 门店行业包含美甲或美睫，包含两者的也计入 |
| 我的门店 | 当前用户曾成功办过会员卡的门店 |

“我的门店”不是收藏过、浏览过或下过订单就算。卡已用完、余额为零或已过期，不影响“曾办过卡”关系；办卡交易撤销、卡关系作废的不计入。

- 未登录点击“我的门店”触发登录；取消登录保持原筛选。
- 全国＋我的门店：展示用户在全国办过卡的门店。
- 城市＋我的门店：仅展示用户在该城市办过卡的门店。
- 切换分类从第一页加载；城市范围和搜索词保持不变。
- “我的门店”与行业标签当前是互斥单选，不同时选择“我的门店＋美发”。

## 七、门店列表排序、分页与缺省

### 7.1 排序规则

有参考位置（LBS）时：

**我的门店优先 → 同组内距离由近到远 → 同距离时店铺评分由高到低 → 门店编号稳定排序。**

没有任何参考位置（LBS）时：

**我的门店优先 → 同组内店铺评分由高到低 → 门店编号稳定排序。**

补充约束：

- 先筛选再排序，“我的门店优先”不突破城市、搜索或行业限制。
- 未登录用户不区分我的门店。
- LBS 与默认收货地址的使用优先级见第三节；收货地址参与排序时仍不展示距离。
- 无评分不伪造 0 分；在评分比较环节排在有评分门店后面。
- 全国模式下仍是“我的门店”优先，因此较远的已办卡门店可能排在较近的非办卡门店前面，这是该优先级的预期结果。

### 7.2 分页规则

- 一期建议首屏 20 家，触底继续加载 20 家。
- 后端先对全部符合条件的门店排序，再分页，不能只排序已加载的本页。
- 同一次连续加载固定查询条件和参考位置，采用稳定分页标识，避免重复或遗漏。
- 全国／城市、分类、关键词变化后清空旧分页，从第一页重新查询。
- 用户关闭定位权限时立即隐藏距离，并按可用参考位置重新查询第一页；开启并取得新定位结果后同样刷新排序。
- 主动重新定位和下拉刷新可更新排序依据；后台位置轻微变化不应让正在浏览的列表突然跳动。
- 从详情返回保留已加载列表及滚动位置；加载完毕显示“没有更多了”。

### 7.3 缺省展示

| 场景 | 文案与操作 |
| --- | --- |
| 全国/城市无上架门店 | “暂无入驻门店，敬请期待”  |
|  |
 |
| 搜索无结果 | “未找到相关门店”＋“清空搜索”；  |
|  |
| 下一页失败 | 保留已有列表，底部提供“重新加载” |
| 已加载全部 | “没有更多了” |


## 八、门店卡片字段、数据来源与异常处理

| 字段 | 数据来源 | 展示和异常规则 |
| --- | --- | --- |
| 门店头图 | 门店资料中的展示图 | 缺失时使用默认图 |
| 门店名称 | 门店资料 | 最多 2 行，超出省略；详情展示完整名称 |
| 可预约标签 | 门店预约配置开关 | 支持预约时展示，不表示当前时段一定有空位；不支持则隐藏 |
| 评分和星级 | 已确定的店铺评分规则计算结果 | 0 条有效评价展示“暂无评分”，不显示星星和分数；1～5 条显示分数并标注“评价较少” |
| 评价数量 | 审核通过且已发布的可见评价数 | 不含系统默认的3条四分评价， 无有效评价不重复显示“0条评价” |
| 门店距离 | 本次有效 LBS 与门店坐标计算的直线距离 | 不使用收货地址或城市中心点生成展示距离 |
| 城市和详细地址 | 门店资料 | 全国模式下地址应包含城市 |
| 营业状态、营业时间 | 门店资料 | 展示营业中／休息中／暂停营业；配置不全不默认营业中 |
| 推荐活动名称 | 门店可售活动；最多展示2个 | 一期默认展示 1 个，单行省略；没有活动不展示 |
| 活动价格 | 门店可售活动 |   -|
| 折扣、划线价 | 门店可售活动 | - |

### 8.1 卡片上的活动选择

- 最多展示2个；取店铺详情页排序的钱2个活动
- 没有活动时，整条活动区域及分割线一起隐藏，不留空白占位。

### 8.2 布局与异常

- 名称过长：最多 1 行省略，标签不可挤压名称到不可读；窄屏可把“可预约”移到下一行。
- 0 条有效评价：展示“暂无评分”，不显示星星和分数；1～5 条：展示分数、评价数量及“评价较少”；6 条及以上正常展示。
- 无 LBS：隐藏距离及关联分隔符，不展示“距离未知”；默认地址仅辅助排序。
- 有 LBS 但门店无坐标：该门店不显示距离，其他可计算门店正常展示。
- 全国模式：地址优先包含城市信息；距离很远时仍可如实展示，不能称为附近门店。
- 用户没有登录：可以浏览、搜索、筛选行业；会员关系和需要登录的操作按登录状态处理。

## 九、典型案例

### 案例 1：未授权定位，首次访问，没有默认地址

默认进入全国；门店列表不限制城市；不显示任何门店距离。未登录时不使用“我的门店”优先，按可用评分和固定编号排序。

### 案例 2：未授权定位，有上海默认收货地址

没有历史浏览城市时，建议初始化为上海。门店可按该默认地址就近排序，但卡片不显示距离；

### 案例 3：已成功定位上海，手动选择全国

左上角展示全国，全国门店均可参与列表筛选；有有效坐标的门店显示到用户的真实距离。我的门店先展示，再在组内按就近排序。

### 案例 4：已定位上海，手动选择杭州

门店列表只展示杭州门店，有条件时展示真实的跨城距离。

### 案例 5：浏览过程中关闭定位

门店列表和活动卡片中的距离立即消失，不再使用旧 LBS。若有默认收货地址，改用其辅助排序；否则跳过距离排序。浏览范围保持用户选定的全国或城市，不强制改变。

## 十、页面分享

### 10.1 入口与分享内容

- 平台首页右上角“…”打开分享菜单，选择“分享给微信好友”；右侧关闭小程序按钮与分享入口分开。
- 分享标题固定为：**发现好门店，遇见更美的自己**。
- 以微信小程序卡片形式分享首页链接，点击卡片进入平台首页，不进入某一家门店。
- 分享封面使用首页公开内容的首屏快照；每次发起分享时重新生成，不截入分享菜单或弹窗。沿用所选全国／城市的公开内容，不含定位距离、搜索历史、会员关系或资产等个人信息。

### 10.2 好友打开后的行为

- 好友打开后默认进入平台首页“全国”，按好友自身的登录状态和定位权限展示实时内容。
- 不传递分享者的登录信息、定位坐标、搜索词、筛选条件或会员身份；快照仅用于预览，不代表好友打开后仍有相同价格、活动或列表顺序。
- 浏览首页及发起分享不要求登录，不强制开启定位。分享是可选行为，不作为使用其他功能的前置条件。

### 10.3 取消、异常与 Demo 演示

- 取消分享或关闭菜单，保留原首页城市、分类及滚动位置，不刷新列表。
- 快照生成失败时，使用预先准备的首页公开内容默认快照；不得阻断浏览。
- Demo 流程：“…” → “分享给微信好友” → 查看标题与首页快照卡片 → “模拟发送给微信好友”；不调用微信、不实际发送消息、不声称真实发送成功。
- 正式小程序使用微信提供的分享入口及好友选择流程；这里只演示产品交互，不自建联系人列表。
