# 流水订单优化

## 1. 说明范围

本文仅说明本次流水订单原型相较原版本的新增与调整内容，不重复商品订单、项目订单等既有业务逻辑。

---

## 2. 流水订单列表页

### 2.1 订单状态口径

- **已完成**：订单正常完成，未发生退款、退卡或作废。
- **部分退款**：项目消费订单内仅部分项目发生退款，其余项目仍有效。
- **已退款**：项目消费订单内所有项目均已退款。
- **已退卡**：新办卡订单执行退卡后更新为该状态；订单操作类型仍为“办卡”。
- **已作废**：商家将整张订单标记为作废；作废本身不代表退款，原支付记录和实付金额继续保留。消费订单作废时，涉及会员卡支付的金额退回会员卡储值余额。

### 2.2 页面结构调整

- 日期快捷筛选调整为：全部、本月、近 3 个月、近 6 个月、筛选。
- 展示列表订单数量统计，统计文案为“共 X 笔流水订单”。订单笔数与条件筛选结果联动
- 列表末尾增加“暂无更多”提示。

### 2.3 筛选弹窗调整

- “自定义”从日期快捷选项中移入筛选弹窗。
- 筛选弹窗新增自定义日期范围：开始日期至结束日期。
- 流水类型筛选支持：会员卡、项目消费。可多选
- 流水状态筛选支持：已完成、退款、已退卡、已作废。可多选
- 类型和状态筛选中不再提供“全部”选项；未选择即代表不限制该条件。
- 筛选结果：不同筛选类型，必须同时满足；同一筛选类型下的筛选项，满足其一即可

### 2.4 订单卡片新增信息

- 新增流水类型标签：通过不同颜色区分“会员卡”和“项目消费”。
- 新增订单状态标签：已完成、部分退款、已退款、已退卡、已作废。
- 会员卡流水新增操作类型标签，支持：办卡、续卡、续费、充值、变更卡、延期。
- “已退卡”：不是订单操作类型，而是办卡订单执行退卡操作后的订单状态。新办卡订单默认状态为“已完成”；执行退卡后，原办卡订单的状态更新为“已退卡”。
- 项目消费订单包含多个项目时，使用“、”分隔：
  - 最多直接展示 2 个项目名称；
  - 超过 2 个时展示为“染发、护发等 3 个项目”。

### 2.5 不同状态的金额展示

#### 已完成

- 保持原有支付方式与合计金额展示。

#### 部分退款

- 合计金额后补充绿色小字退款信息，例如：`合计 ¥368.00（已退款 ¥80.00）`。
- 卡片状态显示“部分退款”。

#### 已退款

- 新增全部退款订单卡片。
- 合计金额后展示全部退款金额，例如：`合计 ¥120.00（已退款 ¥120.00）`。
- 卡片状态显示“已退款”。

#### 已退卡

- 仅适用于操作类型为“办卡”的会员卡订单。
- 保留原办卡订单的实付金额，并在合计金额后展示退款金额，例如：`合计 ¥500.00（已退款 ¥500.00）`。
- 订单操作仍显示“办卡”，订单状态更新为“已退卡”。

#### 已作废

- 作废仅作为订单状态变化，不代表发生退款。
- 保留订单原实付金额，不将金额归零，也不使用删除线。
- 增加说明：“订单已作废，如有疑问可联系店家”。
- 原型已分别提供会员卡操作作废和项目消费作废两类示例。

---

## 3. 「项目消费」订单详情页

### 3.1 通用布局调整

- 移除原有下单消费者个人信息区域。
- 页面主体调整为：项目卡片列表、订单信息、底部实付总额。
- 订单信息默认展开，不再提供展开/收起操作。
- 订单备注使用普通文本展示，不使用输入框样式；无备注时显示“无”。

### 3.2 已完成状态

- 每个项目独立使用一张卡片展示。项目卡片展示：项目名称、原价、服务员工、应付金额和优惠信息。
- 优惠来源拆分为：
  - 店员改价；
  - 会员卡权益折扣；
  - 优惠券抵扣。
- 优惠金额为 `¥0.00` 时不展示该优惠项。
- 包含多个优惠项时，收起状态使用灰色气泡展示优惠摘要，气泡宽度随文字长度变化，箭头位于气泡上方。
- 只有一个优惠项时，不展示优惠摘要气泡；点击“共优惠”及展开箭头后直接查看优惠明细列表。
- 展开状态将优惠项以列表形式展示在灰色区域内；
- 点击优惠摘要、应付金额后的箭头或已展开的优惠区域，可以展开或收起优惠明细。

### 3.3 部分退款状态

- 项目卡片结构与已完成订单保持一致，继续展示原价、优惠、应付和服务员工。
- 仅在发生退款的项目下增加绿色退款记录，例如：“已退款至支付宝 ¥80.00”。
- 未退款项目不增加退款提示。
- 底部仍展示退款前的订单实付总额，不将实付总额直接减为剩余金额。
- “消费明细”弹窗展示：
  - 项目合计：`¥420.00`； - 优惠前订单总价格
  - 实付总额：`¥368.00`； - 优惠后订单总价格
  - 实付金额同一行补充绿色说明：`（已退款 ¥80.00）`；
  - 原支付记录：`支付宝 −¥368.00`。

### 3.4 已退款状态

- 页面结构与部分退款订单保持一致。
- “已退款”表示订单内所有项目均已退款，因此每个项目均展示退款去向和退款金额。
- 底部保留原实付总额 `¥120.00`。
- “消费明细”弹窗保留原支付记录，并显示全部退款金额：
  - 项目合计：`¥120.00`；
  - 实付总额：`¥120.00（已退款 ¥120.00）`；
  - 原支付记录：`支付宝 −¥120.00`。

### 3.5 已作废状态

- 作废状态仅允许作用于整张订单，不支持单个项目作废。
- 页面顶部统一展示“已作废”状态栏及说明：“订单已作废，如有疑问可联系店家”。
- 项目列表仍展示订单内的全部项目、金额及服务员工。
- 未涉及储值卡支付的订单：“消费明细”弹窗及项目卡片上的金额与原有内容保持一致， 无变化
- 涉及储值卡支付订单：
    - “消费明细”弹窗保留原支付记录，并显示全部退款金额：
          - 项目合计：`¥120.00`；
          - 实付总额：`¥120.00（已退回至储值卡 ¥120.00）`；
          - 原支付记录：储值卡 −¥120.00。

---

## 4. 「会员卡」订单详情页

### 4.1 已完成状态

- 详情页新增操作类型。
- 操作类型支持：办卡、续卡、续费、充值、变更卡、延期。
- 底部保留实付总额和支付明细入口。

### 4.2 已作废状态

- 页面顶部与项目消费作废订单使用相同的“已作废”状态栏。
- 继续展示会员卡名称和本次操作类型，例如“护理年卡”“续卡”。
- 作废仅表示商家将订单标记为作废，不代表退款，因此实付金额保持 `¥1,000.00`。
- 底部实付总额保持 `¥1,000.00`。
- “消费明细”弹窗展示原支付记录，无退款信息：
  - 会员卡合计：`¥1,000.00`；
  - 实付总额：`¥1,000.00`；
  - 原支付记录：`微信 −¥1,000.00`。

### 4.3 已退卡状态

- “已退卡”仅适用于操作类型为“办卡”的订单，不适用于续费、充值等其他会员卡操作订单。
- 列表卡片保留“办卡”操作标签，并将订单状态展示为“已退卡”。
- 详情页沿用会员卡详情结构，不单独增加“退款金额”字段；在实付金额下方展示退款结果和退款去向。
- 已退卡案例展示：
  - 实付金额：`¥500.00`；
  - 退款记录：`已退款至微信 ¥500.00`。
- 底部继续展示原实付总额 `¥500.00`。
- “消费明细”弹窗保留原支付记录，并补充：`（已退款 ¥500.00）`。

---

## 5. 「我的卡包」会员卡列表页

### 5.1 多门店展示

- 原页面以单个门店票券容器展示会员卡，本次调整为按门店分组展示。
- 同一消费者可以同时查看自己在多家门店持有的会员卡。
- 每个门店分组展示门店名称、门店标识、门店位置和该门店的会员卡数量。
- 门店分组支持展开和收起；默认展开全部门店。

### 5.2 会员卡信息补充

- 所有会员卡统一展示：卡名称、卡类型和卡号。
- 正常会员卡不展示状态标签，仅已退卡会员卡特殊标记“已退卡”。
- 所有卡片统一使用相同信息结构：顶部展示卡类型和卡名称；已退卡时在顶部追加状态标记，底部通过分割线承载有效期、卡号等补充字段。
- 储值卡展示：卡内余额、使用有效期。
- 所有类型的会员卡均不展示购买价格。
- 计次卡展示：有效期及次数；卡面中心与套餐卡使用相同的“剩余次数 / 购买次数”和“20次 / 26次”组合格式，暂不展示适用项目和适用范围。
- 折扣卡展示：有效期及享受折扣，卡面中心突出折扣信息。
- 套餐卡展示：有效期及次数；卡面中心以“剩余次数 / 购买次数”和“20次 / 26次”的组合格式展示，暂不展示适用项目和适用范围。
- 周期卡展示：有效期及适用范围；适用范围放在卡面中心，项目较多时可省略展示。
- 同一门店内存在多张会员卡时，以独立卡片连续展示。
- 仅储值卡增加“查看消费记录”入口，入口位于卡面中心右侧，与卡内余额处于同一信息层级；其他类型会员卡不展示该入口。

### 5.3 消费者端与商户端的差异

- 消费者端仅展示与本人权益和使用相关的信息。
- 不展示商户端的开单、退卡、升降级、充值、续次、删除等管理操作。
- 门店信息作为会员卡归属标识，地址仅展示到“城市 + 区”，不展示门店距离。

### 5.4 储值卡消费记录

- 新增储值卡消费记录页面，页面顶部展示卡名称、卡类型、卡内余额和卡号；正常状态不展示“使用中”标签。
- 消费记录按日期组织为时间轴；同一天产生的多笔订单归入同一日期分组，每笔记录分别展示具体时间。
- 每条记录仅展示消费时间、项目名称和扣款金额，例如：`10:00　洗剪吹　−¥8.00`。
- 点击单条消费记录可进入对应的流水订单详情页。

---

## 6. Welcome 页面错误兜底

### 6.1 出现场景

- Welcome 页面原本仅包含 DOM 容器，承担解析 `scene`、`q` 短链及业务页面中转，不包含加载态或错误态 UI。
- 中转逻辑成功时进入对应业务页面；接口异常、参数解析失败或其他失败分支直接 `return`，或仅在控制台记录日志，用户会停留在空白页面。

### 6.2 页面展示

- 标题：“哎呀，页面迷路了”。
- 副标题：“可能是服务器出现了一丝波动”。
- 提供“再试一次”按钮，点击后刷新当前页面并重新执行业务中转逻辑。
- 提供“返回首页”按钮，点击后离开 Welcome 空壳页并进入小程序首页。

### 6.3 接入逻辑

- Welcome 页面补充统一错误态 UI，并默认隐藏；业务中转失败时再显示。
- 将所有失败 `return` 和 `catch` 捕获分支统一接入 `showErrorAndFallback()`，禁止静默停留在空壳页面。
- 过期校验接口异常时，根据业务策略选择放行；无法放行时展示提示并引导返回首页，行为需与代码注释保持一致。

---

## 7. 首页切换门店

### 7.1 店铺选择弹窗触发场景

- **默认触发**：用户通过搜索等无店铺参数入口进入小程序首页时，自动打开店铺选择弹窗，并默认选中排序结果中的第一家门店。
- **主动触发**：用户进入首页后点击左上角“切换店铺”，手动打开弹窗；切换前门店作为 T0 优先级固定展示在第一位并默认选中。
- 主动触发是否涉及未登录用户，取决于游客状态是否开放首页切换入口。当前原型按“游客可访问首页且入口可见”处理，因此未登录用户也可主动切换，并复用未登录列表规则。
- 用户通过订单页面、店铺商品详情页、店铺主页二维码或店铺员工分享码等携带明确店铺参数的入口进入首页时，直接使用来源店铺，不展示切换门店弹窗。
- 店铺参数无效、店铺已停用或无法解析时，回退至默认触发流程。

### 7.2 店铺列表展示规则

#### 主动触发通用规则

- 切换前门店为 T0 优先级，固定展示在列表第一位。
- 除 T0 门店外，其余门店再根据登录状态、建档状态和定位状态排序。

#### 未登录用户

- 已授权定位：仅展示附近门店，按 LBS 距离从近到远排序。
- 未授权定位：展示后台配置的 60 家兜底门店，按系统默认顺序排列。
- 未登录状态不查询、不展示已建档门店。

#### 已登录但未在任何门店建档

- 已授权定位：展示当前所在城市门店，按 LBS 距离从近到远排序；城市仅作为后台过滤条件，不在列表中单独展示城市层级。
- 未授权定位：展示 60 家兜底门店，按系统默认顺序排列。

#### 已登录且已在一家或多家门店建档

- 已授权定位：第一优先级展示已建档门店，按建档时间降序排列；第二优先级展示附近其他门店，按 LBS 距离从近到远排列。
- 未授权定位：第一优先级展示已建档门店，按建档时间降序排列；第二优先级展示 60 家兜底门店，按系统默认顺序排列。
- 已建档门店不受城市或距离范围限制。

#### 定位与兜底

- 未取得定位权限时，先调用小程序系统定位授权，申请方展示为“剑琅美遇 申请”。
- 用户拒绝授权、系统定位不可用或无法重新开启时，进入 60 家兜底门店列表；同时保留按“省份－城市”手动选择门店范围的能力。
- 60 家兜底门店不展示距离，也不参与 LBS 排序。
- 兜底页面保留“授权定位查看附近门店”按钮，可重新发起定位授权。

### 7.3 通用交互规则

- 店铺列表为单选模式，弹窗打开时必须默认选中一家门店。
- 不支持取消选中；点击其他门店时，将选中状态切换至新门店并关闭弹窗，进入对应店铺主页。
- 默认触发时选中排序结果第一家门店；主动触发时默认选中切换前门店。
- 点击“确认”后按当前选中门店关闭弹窗并进入对应店铺主页。
- 点击蒙层或关闭按钮时，如已有默认选中门店，则按当前选中项进入；如理论异常导致无选中项，则保持弹窗开启。

### 7.4 特殊场景

- 老用户仅有一家已建档门店时，直接进入该店铺主页，不展示选择弹窗。
- 老用户有多家已建档门店时，优先弹出店铺列表供用户选择。
- 特殊场景优先级：有效店铺参数直达 > 老用户单店直达 > 其他默认触发规则。

### 7.5 核心设计原则

- **减少操作**：始终默认选中一家门店，避免空选状态。
- **建档优先**：已建档用户优先展示关联门店，强化用户归属。
- **就近优先**：获得定位权限时，其他门店按距离排序。
- **兜底保障**：无定位权限时使用 60 家固定门店列表，保证首页可以继续使用。
